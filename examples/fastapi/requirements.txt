# Install this repository first with: python -m pip install -e .
# Keep this file limited to the web demo's external dependencies so it
# never resolves the upstream PyPI sllurp package by accident.
fastapi>=0.115.8
pydantic>=2.10.6
uvicorn>=0.34.0
websockets>=15.0
